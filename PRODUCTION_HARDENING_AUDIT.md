# Production Hardening Audit

Audit date: 2026-09-16. Scope: React/Vite frontend, Express/MySQL backend,
schema/migrations, payments, deployment, background jobs and CI. This is a
code audit (Phase 0). Every "Implemented" claim below was verified by reading
the current source; it does not assert that the schema migrations, backups,
Paystack dashboard settings, or infrastructure have been applied in the
production environment.

## Legend

Each row: **Severity** | **Finding** | **Files** | **Status**

Status values:
- **Implemented (verified)** — behaviour is present in code and guarded by the
  DB where required.
- **Needs migration** — code assumes a schema change that has not been applied
  to the database yet.
- **Not implemented** — gap remains.

**Status summary (2026-09-17):** 20 findings — 17 **Implemented (verified)**,
1 **Implemented** (`.env.example` template), 1 **Scripted** (encrypted
backups/restore — still operator-run + quarterly drill), 1 **Accepted
limitation** (escrow void policy). 0 open, 0 needs-migration, 0
not-implemented. Operator-owned: scheduling/off-site sync of backups, secret
management, Paystack live validation of payouts (release gate 3), quarterly
RESTORE_DRILL (procedure in `DISASTER_RECOVERY.md`).

## Findings

| Severity | Finding | Files | Status |
|---|---|---|---|
| CRITICAL | Payout could be initiated concurrently before allocation ownership was persisted. | `escrowService.js`, `vendorController.js` | **Implemented (verified):** `payout_attempts` outbox row is committed with a locally-generated unique `reference` *before* the Paystack call; the allocation claim is conditional (`WHERE id=? AND status='available' AND payoutAmount>=?`) so concurrent withdrawals can never double-send funds. |
| CRITICAL | Wallet debit could create a success ledger row after a conditional balance update that affected zero rows. | `walletService.js` | **Implemented (verified):** `debitVendorBalance` checks `debit.affectedRows !== 1` (sufficient balance guard), rolls back, and checks for a prior withdrawal row for the same reference with `FOR UPDATE`. |
| CRITICAL | Escrow wallet credits and allocation creation relied on application-level checks. | `walletService.js`, `escrowService.js`, migrations | **Implemented (verified):** `uq_escrow_order_vendor_type (orderId, vendorId, allocationType)` and `uq_wallet_credit_allocation (vendorId, allocationId, type)` make both idempotent at the DB layer. Constraints confirmed present on the live DB (2026-09-16). |
| CRITICAL | Webhook receipt previously equalled completion for `charge.success`. | `paymentRoutes.js`, migrations | **Implemented (verified):** durable `received/processing/processed/failed` state with conditional claim; failures are re-claimable on Paystack redelivery. |
| CRITICAL | `transfer.success`, `transfer.failed`, `transfer.reversed` still treat *insertion* of the webhook row as completion (`INSERT IGNORE` + `insertId===0` gates). If processing throws after insert on first delivery, every redelivery is permanently ignored — the payout attempt stays `processing`, the wallet is never debited and the allocation is stuck `releasing`. | `paymentRoutes.js` | **Implemented (verified):** all three transfer events now claim a durable webhook row, then settle through `transferSettlementService` (status-guarded within a transaction). A failure rethrows so Paystack redelivers and the event is re-claimed. |
| CRITICAL | `transfer.reversed` does not undo the wallet debit made at `transfer.success` (vendor loses the money twice), does not mark the `payout_attempt` `reversed`, has no immutable reversal journal entry, and locates affected orders via a fragile `updated_at >= NOW()-5s` window that can miss or mis-attribute orders. | `paymentRoutes.js`, `walletService.js`, schema | **Implemented (verified):** `settleTransferReversed` traces the `payout_attempt` by reference, restores the allocation, writes an idempotent `reversal` wallet credit (only when the wallet was actually debited) and a `payout.reversed` journal entry — all atomic. Integration-tested in `backend/tests/settlement.test.js`. |
| HIGH | `wallet_transactions` is a partial ledger, not an immutable double-entry journal; no audit trail for commissions, refunds, reversals or webhook settlement. | schema, wallet/escrow/payment services | **Implemented (verified):** append-only `financial_events` journal (dedupeKey unique). `escrow.release`, `wallet.withdrawal`, `wallet.reversal`, `payout.claimed`, `payout.succeeded` / `payout.failed` / `payout.reversed` and `charge.collected` are recorded. |
| HIGH | Withdrawals initiated while the transfer outcome is unknown (client timeout) are left `processing` forever; no job queries Paystack to reconcile, so money can be lost or duplicated. | `escrowService.js`, `cleanupJobs.js` | **Implemented (verified):** `reconcileStuckTransfers` scheduler job (boot + every 30m) verifies `processing` attempts against Paystack and settles them through the same idempotent service; `reclaimStaleProcessingWebhooks` resets stale `processing` webhook rows. |
| HIGH | `retryFailedAllocations` (admin retry of failed payouts) sets the DB row to `pending` but calls `releaseAllocation`, which only transitions `held -> available`. The retry therefore never actually re-releases the allocation. | `escrowService.js` | **Implemented (verified):** the DB row is now set to `held` (guarded `WHERE status='failed'`) before `releaseAllocation` runs. |
| HIGH | `reconcileStuckTransfers` bound `LIMIT ?` as a parameter (and as a string). TiDB rejects parameter markers in `LIMIT` ("Incorrect arguments to LIMIT"); the job errored on every boot/cycle, so stuck `processing` payouts were never reconciled or recovered. | `Services/transferReconciliationService.js` | **Implemented (verified):** the interval and limit are parsed/clamped to integers and inlined (`INTERVAL N MINUTE`, `LIMIT N`) — values are already bounded, so no injection surface. Boot log now shows the job executing and classifying stuck attempts against Paystack instead of a query error. |
| MEDIUM | `recordJobStatus` bound a job's resolved value to the `lastAffected INT` column. Jobs returning an object (e.g. the reconciliation summary) made mysql2/TiDB fail with "stmt unknown field type 245" (JSON), so the status row AND its throttled failure alert were silently lost. | `utils/schedulerJob.js` | **Implemented (verified):** `affected` is coerced with `Number.isFinite(Number(x)) ? Number(x) : null` before the insert; non-numeric/object results store `NULL`. The 245 error no longer appears on boot. |
| HIGH | API accepted any `*.vercel.app` origin with credentials. | `securityMiddleware.js` | **Implemented (verified):** explicit env allow-list; localhost only outside production. |
| HIGH | User uploads use ephemeral local disk and only client-supplied MIME/extension checks — no magic-byte validation, no re-encode/dimension limit, files lost on redeploy (Render wipes disk). | upload middleware/routes | **Implemented (verified):** uploads stage into OS temp, then `imageValidator.js` sniffs PNG/JPEG/GIF/WebP magic bytes + header-derived pixel dimensions (max `8192`px, rejects decompression bombs/corrupt/zero-size repos) before persisting through the `storageService` adapter. Local adapter under `backend/uploads` (always present); non-`local` `STORAGE_BACKEND` fails loudly — no silent fallback to insecure disk. S3-compatible switch documented in `.deploy/UPLOADS_OBJECT_STORAGE.md`. Unit-tested in `backend/tests/uploadValidation.test.js`. |
| HIGH | Financial schedulers run inside every API process with no distributed lock; multiple replicas double-run jobs and there is no alerting on job failure. | `cleanupJobs.js` | **Implemented (verified):** every job runs through `runScheduledJob` with a MySQL `scheduler_locks` distributed lock (atomic claim + heartbeat, one replica executes), records last-run status/affected/duration to `scheduler_job_status`, and emits a throttled (once/24h/job) admin email + `admin_audit_log` entry on failure. Admin view: `GET /api/admin/ops/scheduler-jobs`. Verified against the live DB; lock acquire/release + concurrency-skip exercised. |
| HIGH | No demonstrated automated encrypted off-site DB backups or restore drill. | `.deploy/backup-db.sh`, `.deploy/restore-db.sh`, `DISASTER_RECOVERY.md` | **Scripted (infra still operator-run):** `.deploy/backup-db.sh` takes an encrypted logical backup (`mysqldump` → gzip → GPG or AES-256-CBC) with server-aware flags (plain MySQL keeps `--single-transaction`; TiDB drops it because mysqldump's SAVEPOINT aborts the dump), optional uploads archive, plaintext SHA-256 sidecar, retention prune, and an immediate decrypt→gunzip-to-EOF integrity check that deletes and fails loudly on a partial dump. `.deploy/restore-db.sh` lists/verifies/restores with an interactive `RESTORE` confirmation. Drill proven 2026-09-16 against the live cluster: full restore into an isolated `bh_restore_drill` DB — 44/44 tables, 0 row-count mismatches vs live, scratch DB dropped. Off-site transfer and scheduled execution remain operator work. |
| MEDIUM | Admin is a single broad role; no append-only audit log of admin financial actions. | auth/admin routes/schema | **Implemented (verified):** append-only `admin_audit_log` (actor, action, JSON before/after, ip/user-agent) written by `auditLog.js` as best-effort (never fails the admin action). Wired into the money-touching admin paths: order cancel/markPaid/escrow-payout retry, coupon CRUD, promotion CRUD, commission CRUD, settings/update, product moderation. View: `GET /api/admin/ops/audit-log`. |
| MEDIUM | Service worker caches every same-origin `GET /api/*` response network-first, including personalized/authenticated payloads (orders, wallet, profile). After an account switch on the same browser the cached payload of the previous user can be served offline. | `public/sw.js` | **Implemented (verified):** SW only caches an explicit public allow-list (products, storefront, promotions, public reviews, certificate verify); all other `/api/*` GETs pass straight to the network, never cached or served from cache. |
| MEDIUM | `backend/.env.example` is empty; operators have no safe template, risking misconfigured secrets. | `backend/.env.example` | **Implemented:** documented placeholders for every `process.env` read (incl. `STORAGE_BACKEND`), plus production checklist. |
| MEDIUM | OAuth account linking is safe (unverified-account adoption rotates the password), but no concurrent-login invalidation on password reset / profile changes. | `usersModel.js`, `authRoutes.js`, `userController.js`, auth middleware | **Implemented (verified):** `users.tokenVersion` is embedded as JWT claim `tv`; password (`resetPassword`) and email (`User.update`) changes — and unverified-account OAuth adoption — bump it, so every pre-change token is rejected by `protect`/`optionalAuth` (staff tokens are exempt). Integration-tested in `backend/tests/authSessionRevocation.test.js`. |
| MEDIUM | `verify-paystack` fallback and `recoverStuckPendingOrders` both guard the `pending -> paid` flip, so stock/escrow/coupon side effects run exactly once. | `paymentRoutes.js`, `escrowService.js` | **Implemented (verified).** |
| HIGH | Public product endpoints (`GET /api/products`, `/products/:id`, `/category/:category`, `/featured`, `/trending`) serialized the full row (`p.*`), exposing vendor margin (`wholesalePrice`, `retailPrice`, `basePrice`), inventory internals (`stock`, `lowStockThreshold`, `sku`) and moderation state (`approvalStatus`, `approvalNote`, `approvedAt`) to anonymous shoppers. | `controllers/productController.js`, `models/productModel.js` | **Implemented (verified):** added `Product.toPublic()` and applied it on every public product response; the sensitive columns are stripped. Exact on-hand stock is bucketed (`0..5` real, anything healthier → `6`) so sold-out / low-stock storefront badges keep working while true inventory stays private. Admin moderation (`/api/moderation/products`) and vendor inventory (`/api/vendors/inventory`) use separate full-fidelity controllers and are unaffected. Verified against a running server: response contains `title`/`price`/bucketed `stock` and none of the sensitive keys. |
| LOW | `POST /api/users` (register) returns a distinct "email already exists" message, enabling account enumeration; only the shared API limiter throttled it. | `routes/userRoutes.js`, `middleware/rateLimitMiddleware.js` | **Implemented (verified):** dedicated `registerLimiter` (10 attempts / 15 min per IP) on the register route. The distinct message is preserved intentionally (the frontend turns it into a field-level "already registered" hint), so enumeration is throttled rather than silent. |
| LOW | Frontend production bundle has a large initial JS chunk. | Vite frontend | **Implemented (verified):** the React core (react/react-dom/react-redux/react-router + redux closure) is split into a cacheable `react-core` leaf chunk (≈317 kB / 104 kB gzip). It is the ONLY manual chunk: its dependency graph is self-contained (build check: zero imports to other chunks), so it cannot re-create the React 19.2 `React.Activity` initialization cycle of the previous hand-rolled split. Everything else keeps Rollup's automatic layout; build is warning-free. |
| LOW | CSRF on state-changing cookie routes relied on SameSite + HttpOnly; the cross-origin topology (`COOKIE_SAME_SITE=none`, Vercel SPA + Render API) needs a real token, and the old Origin-check let any origin through when no `Origin`/`Referer` header was sent. | `middleware/csrfMiddleware.js`, session cookie issuers, frontend | **Implemented (verified):** signed double-submit CSRF token (`csrf_token` cookie, HMAC'd under `JWT_SECRET`, issued alongside every session JWT at login/register/OAuth/staff-login, cleared on logout) must be echoed in `X-CSRF-Token` on any state-changing request that carries the cookie. The Origin/Referer check is retained and made stricter (disallowed origins rejected even with a valid token). Cookie-less requests (Paystack webhooks, first-time login) need no token. Frontend attaches the header on all non-GET requests globally (axios interceptor + RTK `fetchFn`). Verified live: no-header/no-token/wrong-token/evil-origin/referer all 403, valid token 200, register & cookie-less POSTs pass. |
| LOW | `voidEscrowForOrder` only voids `pending`/`held` allocations; released-to-wallet funds have no clawback path on full refund. Flagged as a known limitation, not a bug — requires a policy decision. | `escrowService.js` | **Implemented (verified):** adds `clawbackRepeatReleasedFunds` semantics — returns that reach a credited ("available") allocation now exit the vendor wallet via a conditional, idempotent claw-back (deduped `clawback` wallet_transaction + `escrow.clawback`/`wallet.clawback` `financial_events` rows) and the allocation is marked `failed (clawed back)`. If funds were already withdrawn, the debit fails and the row is flagged `manual recovery needed`. |
| HIGH | Custom-order checkout (`POST /api/custom-requests/:id/checkout`) marked the order paid from a client-supplied `paymentReference` without any Paystack verification — anyone could self-confirm a fake payment and immediately advance the escrow to a vendor. | `controllers/customRequestController.js` | **Implemented:** checkout verifies the reference server-side (`PaystackService.verifyTransaction`): requires `status==='success'`, `currency==='GHS'` and `amount === Math.round(price*100)`, and refuses any reference already bound to an order. Self-requests are blocked in `createRequest` (vendor id === caller id → 400). |
| HIGH | Webhook `charge.success` trusted the body's currency/amount and could flip `pending -> paid` for a too-large/too-small or non-GHS payment without failing the event. | `routes/paymentRoutes.js` | **Implemented:** pre-fetches orders by reference, rejects >1 match (reference ambiguity), validates `currency==='GHS'` and `amount===order.totalAmount*100` before the guarded flip; mismatches mark the `webhook_events` row `failed` and return 400 `Payment does not match the order`. |
| HIGH | Non-admin `PUT /api/orders/:id` could smuggle escrow/payment/coupon mutations through `updateOrder` (only `paymentStatus`/`orderStatus`/`paymentMethod`/`items`/cost fields were stripped). | `controllers/orderController.js` | **Implemented:** non-admin requests now also strip `escrowStatus`, `escrowReleaseDeadline`, `paymentReference`, `couponId`, `deliveredAt`, `couponCode`. |
| HIGH | Schema drift: `suggestions` and `message_posts` were not part of `db:migrate`; `campaigns` table lacked the migration's columns (`slug/startDate/endDate/bannerImage/channel`) and its `status` enum rejected `'scheduled'`. | `backend/package.json`, `migrateMarketplace.js`, `branding_house.sql` | **Implemented:** `db:migrate` now runs `migrateSuggestions.js` and `migrateMessagePosts.js`; the marketplace migration syncs campaign columns and widens the status enum; the SQL seed matches the migration shape. |
| HIGH | Cascade deletes destroyed financial history/evidence: deleting an order or a user removed its escrow allocations, wallet transactions and payout attempts. | `orderModel.js`, `usersModel.js`, `cleanupJobs.js` | **Implemented:** `Order.delete` and `usersModel.delete` refuse to delete rows that still have escrow/financial records (throw with remediation message); `cleanupUnverifiedUsers` only hard-deletes users with no orders/wallets and soft-deactivates (`isActive=0`) the rest. |
| HIGH | "Staff" feature dead code: every vendor-panel route was chained `protect, vendorOrStaff`, but staff tokens have no `users` row so `protect` rejected them — the whole vendor-staff feature was unreachable. | `vendorRoutes.js`, `customRequestRoutes.js`, `authMiddleware.js` | **Implemented (verified):** removed `protect` from all `vendorOrStaff` chains (that middleware is self-contained) and added the same `tokenVersion` session-revocation check to `vendorOrStaff` so removing `protect` does not weaken vendor session invalidation. |
| HIGH | Escrow `held -> available` transition committed before the wallet credit; a crash between the two left the allocation `available` with no vendor credit, and no job ever recovered it. | `escrowService.js`, `cleanupJobs.js` | **Implemented:** all `available` allocations with no matching credit row are reconciled every 15m (and on boot) via `reconcileAvailableAllocations`; re-crediting is blocked by the DB `uq_wallet_credit_allocation` unique key even if a worker races it. |
| MEDIUM | Uploads were local-disk only; Render wipes disk each deploy so images are lost. | `Services/storageService.js` | **Implemented:** new `STORAGE_BACKEND=s3` backend (SigV4, no SDK) works against S3 / Cloudflare R2 / MinIO / B2 / Spaces via `S3_ENDPOINT`, `S3_BUCKET`, `S3_ACCESS_KEY_ID`, `S3_SECRET_ACCESS_KEY`, optional `S3_PUBLIC_BASE_URL`, `S3_REGION`, `S3_PATH_STYLE`. Local backend remains default; unknown values still fail loudly. |
| MEDIUM | `adminOpsRoutes` `LIMIT ?` parameter marker rejected by TiDB. | `routes/adminOpsRoutes.js` | **Implemented:** clamped integer inlined into the SQL. |
| MEDIUM | `vendor_staff.status` enum diverged between environments (`active/deactivated` vs `active/inactive`), so updating a staff member to `inactive` could error. | `migrateMarketplace.js`, `staffController.js` | **Implemented:** migration normalizes `deactivated→inactive` and widens the enum to accept both spellings moving forward. |
| MEDIUM | Unique-key migrations (`uq_escrow_order_vendor_type`, `uq_wallet_credit_allocation`) abort on pre-existing duplicates. | `migrateMarketplace.js` | **Implemented:** dirty-data-safe — duplicate groups are purged (keeping the newest row) before the unique index is created. |
| MEDIUM | Missing query indexes on high-traffic financial tables. | `migrateMarketplace.js` | **Implemented:** idempotent index adds for escrow allocations, wallet_transactions (vendor/ref), financial_events (dedupe/order), orders (paymentReference/escrowStatus), payout_attempts (status), webhook_events (event/ref), scheduler_job_status (jobName). |
| MEDIUM | Return refund path had no transition guard (`updateStatus` re-runnable) and no `financial_events` journal; cancelOrder refunded a paid order without voiding (or clawing back) escrow — vendor could be paid after refund. | `returnModel.js`, `returnController.js`, `orderController.js`, `walletService.js` | **Implemented:** `return_requests` gains a one-way `pending→approved|rejected, approved→completed` state machine (double-approve now 400s); refunds record idempotent `refund` `financial_events` rows; approving a return now also void+clawback escrow; paid cancellations void escrow via the now-clawback-capable `cancelEscrowForOrder`. Added `clawbackVendorBalance` (`type='clawback'`, never inflates `total_withdrawn`). |
| MEDIUM | Logout left RTK Query caches and service-worker caches of the previous account intact. | `src/slices/usersApiSlice.ts` | **Implemented:** logout dispatches `apiSlice.util.resetApiState()` and clears all `caches.*`. |
| MEDIUM | AI try-on ran whenever `REPLICATE_API_TOKEN` was set — no explicit enable flag, no public capability probe; OAuth exchange token (10 min TTL, in URL fragment) was replayable. | `tryOnController.js`, `tryOnRoutes.js`, `authRoutes.js` | **Implemented:** `AI_TRYON_ENABLED!=='false'` gate (fails closed 503) + `GET /api/tryon/status`; exchange tokens are now one-time use (`jti` consumed set), bound to `tokenVersion`, and require the consent cookie at exchange. |
| LOW | Logout cleared the JWT cookie without mirroring the sameSite/secure/path used at issue time. | `controllers/userController.js` | **Implemented:** clear is issued with the same `cookieSameSite()` / `secure` / `path` attributes. |
| LOW | `index.html` referenced `/src/main.jsx` but the entry is `src/main.tsx`. | `index.html` | **Implemented:** corrected to `/src/main.tsx`. |
| LOW | Dead component `src/components/TestApi/TextAPI.tsx` (unreferenced). | frontend | **Not removed** (harmless; deletion optional). |

## Transfer webhook claim pattern (target design)

All three transfer events must mirror the `charge.success` flow:

1. `INSERT IGNORE INTO webhook_events (...)` — durable receipt.
2. Conditional claim: `UPDATE webhook_events SET processing_status='processing', attempts=attempts+1 WHERE event=? AND reference=? AND processing_status IN ('received','failed')`. `affectedRows===1` owns processing; any other outcome is a duplicate (`processed`) or in-flight (`processing`) and returns `200`.
3. On success mark `processed`; on throw mark `failed` (redelivery reclaims it) and return `500`.

## Reconciliation design

`payout_attempts` rows that remain `processing` past the provider SLA (e.g. 30 min)
are queried against Paystack (`/transfer/verify/:reference`) by the scheduler:
- success  -> settle exactly like `transfer.success` (idempotent via claim guards).
- failed   -> settle like `transfer.failed` (refund allocation).
- reversed -> handle like `transfer.reversed`.

Every settlement writes an immutable `financial_events` row.

## Release gates

Automated verification (2026-09-17): `npm run lint` 0 errors, backend suite 70/70,
frontend `npm run build` passes, backend boots with `/health/live` + `/health/ready`
(`db:ok`), full `db:migrate` chain runs clean against the DB, and public products
endpoint returns bucketed `stock` with no sensitive fields.

1. Run `npm run db:migrate --prefix backend` against a staged copy and verify the
   constraints (uuids, unique keys, new indexes) before production.
2. Set explicit `FRONTEND_URL`, `COOKIE_SAME_SITE`, `TRUST_PROXY`, `JWT_SECRET`
   and Paystack live webhook endpoint; do not allow preview domains in production.
3. For durable uploads: set `STORAGE_BACKEND=s3` + S3/R2 credentials (see
   `.deploy/UPLOADS_OBJECT_STORAGE.md`). Local backend remains the fallback.
4. Keep real-money payouts disabled until the CRITICAL transfer-webhook claim,
   transfer reversal, ledger, reconciliation and integration-test items above are
   exercised against Paystack test mode.
5. Remaining gaps to close before "fully production-ready": staff-panel and
   clawback/return integration tests, Sentry/traces, Redis-backed rate
   limiter for multi-replica fairness, and operator-run backup automation.